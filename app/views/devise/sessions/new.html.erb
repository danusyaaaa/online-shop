<div class="container_in_sign_up">
<h1>Войдите в систему.</h1>
<br>

<%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
  <div class="field">
    
    <%= f.email_field :email,placeholder:"Email", autofocus: true, autocomplete: "email",class:"form-control log_in" %>
  </div><br />
<br />
<br />
  <div class="field">
    
    <%= f.password_field :password,placeholder:"Пароль", autocomplete: "current-password",class:"form-control log_in" %>
  </div>

  <% if devise_mapping.rememberable? %>
    <div class="field">
      <%= f.check_box :remember_me %>
      <%= f.label :remember_me %>
    </div>
  <% end %><br>
 
<div class="as-signin-info">
   
Адрес электронной почты, которым вы пользуетесь для доступа к Shop.
</div>
  <div class="actions">
    <%= f.submit "Вход" ,class:"btn btn-primary btn_log_in"%>
  </div>
<% end %>
<br>
<br>
<br>
<%= link_to "У вас нет аккаунта ? Создайте аккаунт .", new_registration_path(resource_name),class:"btn_color_sign_up" %><br />
</div>